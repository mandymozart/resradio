import styled from "@emotion/styled";
import TempoRangeSlider from "../RangeSlider/TempoRangeSlider";

const Container = styled.div`

`;

export const TempoFilter = () => {
    return (
        <Container>
            <TempoRangeSlider />
        </Container>
    )
}

